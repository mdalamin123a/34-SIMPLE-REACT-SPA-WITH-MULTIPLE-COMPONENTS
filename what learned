1. Installing react-bootstrap and use react-bootstrap component
  ==> npm install --save react-bootstrap
  ==> import {Button} from 'react-bootstrap';
  ==> function App() {
    return (
      <Button>Button</Button>
    );
  }